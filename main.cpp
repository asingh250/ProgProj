#include <iostream>
#include "include/salesManager.h”
 
 
int main() {
    //loads info from text files
    retrieveInfo();
    //prints the menu and takes commands until q is typed
    printMenu();
